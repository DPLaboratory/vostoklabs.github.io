// The grouped symbol library moved into `@vostok/fonts` (`symbolGroups.ts`) the day a
// second app — the fold-up box's logo field — wanted the same dozen human-named groups
// over the same glyphs. One copy, so "Popular" means the same thing everywhere.
export { POPULAR_IDS, POPULAR, QUICK_PICKS, SYMBOL_GROUPS, searchGroup, type SymbolGroup } from '@vostok/fonts';
